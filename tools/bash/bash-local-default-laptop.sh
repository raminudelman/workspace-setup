#!/usr/bin/env bash

# Load here the common local bash configurations for the current profile
if [ -f "${HOME}/.config/bash/bash-local-default-common.sh" ]; then
    source "${HOME}/.config/bash/bash-local-default-common.sh"
fi

# Put here any local bash configurations
