# Load common Tmux local configuration file
if-shell "[ -f ~/.config/tmux/tmux.conf.common ]" 'source ~/.config/tmux/tmux.conf.common'

# Increase the length of status bar, or the characters might not fit
set -g status-right-length 80
